import styled from "styled-components";

export const CurrencyMethodField = styled.div`
  display: flex;
  align-items: center;

  gap: 0.25rem;

  input[type="radio"] {
    height: 1.25rem;
    width: 1.25rem;
  }
`